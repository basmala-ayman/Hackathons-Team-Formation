import styles from "./TeamCard.module.css";
import { TeamIcon  } from "../../../assets/Icons";
function TeamCard({
  teamName,
  hackathonName,
  description,
  members = [],
  maxMembers = 4,
  onAccept,
  onView,
}) {
  return (
  

);
}

export default TeamCard;
