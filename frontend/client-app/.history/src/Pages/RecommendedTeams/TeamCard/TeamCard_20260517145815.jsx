import styles from "./TeamCard.module.css";
function TeamCard({
  teamName,
  hackathonName,
  description,
  members = [],
  maxMembers = 4,
  onAccept,
  onView,
}) {
  return <div></div>;
}

export default TeamCard;
